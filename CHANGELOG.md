# Changelog

## Unreleased

### New Features

- **Exploration quality support**: Added support for `exploration` as a new build quality option via `--quality exploration`. The `exploration` quality is a pre-release quality between `insider` and nightly builds. It is supported across all runtimes (desktop, web, vscode.dev) and all platforms (macOS, Linux, Windows).

## 1.0.17

- Previous release.
